# Stripe Configuration
# Copy this file to .env.local and replace with your actual keys

# Get your publishable key from: https://dashboard.stripe.com/apikeys
# For testing, use a key starting with 'pk_test_'
# For production, use a key starting with 'pk_live_'
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Get your secret key from: https://dashboard.stripe.com/apikeys
# Only needed if you implement the backend API route (pages/api/checkout.js)
# For testing, use a key starting with 'sk_test_'
# For production, use a key starting with 'sk_live_'
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
